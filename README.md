# 🎬 VidRAG — Chat with YouTube Videos using RAG + LangChain LCEL + FastAPI

## 📖 Overview
**VidRAG** (Video Retrieval-Augmented Generation) is an AI system that allows you to **ask questions about YouTube videos** and receive **accurate, context-grounded answers** — all powered by **LangChain**, **OpenAI**, **Pinecone**, and **FastAPI**.

Instead of watching an entire video, you can now simply ask:
> “What are the key points discussed?”  
> “When does the speaker explain transformers?”  
> “Summarize the conclusion section.”

VidRAG intelligently retrieves transcript segments, finds semantically relevant chunks, and uses **RAG (Retrieval-Augmented Generation)** to generate timestamped, factual responses.

---

## 🚀 Features

✅ **Real-time Q&A** over YouTube videos  
✅ **Token-based text splitting** for better chunking  
✅ **Pinecone** vector database for semantic search  
✅ **LangChain LCEL (Runnable pipeline)** for clean orchestration  
✅ **FastAPI** backend with Swagger UI  
✅ **Timestamped and context-backed responses**  
✅ **Modular architecture** — easy to extend or replace components  

---

## 🧠 Architecture

📁 VidRAG/
│
├── modules/
│ ├── loader.py # Loads YouTube transcripts
│ ├── splitter.py # Splits text into token-aware chunks
│ ├── embeddings.py # Generates embeddings using OpenAI
│ ├── vector_store.py # Stores embeddings in Pinecone
│ ├── retriever.py # Retrieves top-k similar chunks
│ └── rag_chain.py # Constructs and runs the RAG pipeline
│
├── app.py # FastAPI app for serving VidRAG
├── requirements.txt # Project dependencies
└── README.md # Project documentation



---

## ⚙️ Tech Stack

| Component | Technology |
|------------|-------------|
| **Language** | Python 3.10+ |
| **Framework** | FastAPI |
| **LLM Orchestration** | LangChain (LCEL, Runnables) |
| **Embeddings** | OpenAI (text-embedding-3-small) |
| **Vector Database** | Pinecone |
| **Transcript Fetching** | YouTube API |
| **Frontend Docs** | Swagger UI (auto-generated by FastAPI) |

---

## 🧩 How It Works

1. **Load Transcript:**  
   Extracts the transcript from a YouTube video using its URL.  

2. **Chunk Text:**  
   Splits the transcript into semantically meaningful, token-based chunks.  

3. **Embed Chunks:**  
   Generates embeddings using `text-embedding-3-small`.  

4. **Store in Vector DB:**  
   Saves embeddings in Pinecone for efficient semantic search.  

5. **Retrieve Similar Chunks:**  
   Uses a similarity-based retriever (`k=2` or configurable).  

6. **Generate Answer:**  
   Uses GPT-4o-mini via LangChain LCEL to compose answers grounded in retrieved context.  

7. **Serve via FastAPI:**  
   Exposes endpoints like `/ask` for user queries with responses.

---

## 🛠️ Installation & Setup

### 1️⃣ Clone the repository
```bash
git clone https://github.com/BilalAhmed7072/VidRAG.git
cd VidRAG

2️⃣ Create a virtual environment
python -m venv venv
source venv/bin/activate   # On Linux/Mac
venv\Scripts\activate      # On Windows

3️⃣ Install dependencies
pip install -r requirements.txt

4️⃣ Set up environment variables
Create a .env file in the project root with the following keys:

OPENAI_API_KEY=your_openai_api_key
PINECONE_API_KEY=your_pinecone_api_key
YOUTUBE_API_KEY=your_youtube_api_key
PINECONE_INDEX_NAME=vidrag-index

5️⃣ Run the FastAPI server
uvicorn app:app --reload
